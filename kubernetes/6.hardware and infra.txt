-nodes ,including the master can be physical or VM running on kubernetes components and a container management such as docker or rocket
-the nodes should be connected by a network ,through the internet will work as long as port 443 and wotever the pod networking system uses are unblocked
- a pod networking application like flannel is needed to communicate with one another,and it makes use of a overlay network(by default its vxlans) to provide that servce